# Quiz — Helm Basics

1) Helm is:
   a) container runtime
   b) Kubernetes package manager
   c) service mesh

2) A release is:
   a) a chart folder
   b) an installed instance of a chart
   c) a Kubernetes node

3) Command to render manifests without installing:
4) File that contains default values:
5) Command to show release history:
6) How to override a value quickly:
7) One reason Helm is useful in multi-env setups:
8) One safe secrets-management approach:
