# Module 08 — Best Practices

- Use helpers for consistent names/labels
- Keep charts generic and configurable
- Separate values by environment (values-dev.yaml, values-prod.yaml)
- Always run `helm lint` in CI
- Avoid secrets in Git
